
## Active Technologies
- Java 21 (Virtual Threads via `spring.threads.virtual.enabled=true`) + Spring Boot 3.x, Spring Data JPA, Spring Cache, Spring WebClient, (001-chimera-pipeline)
- PostgreSQL 16 (all persistent entities), Redis 7 (TrendReport cache, 24hr TTL) (001-chimera-pipeline)

## Recent Changes
- 001-chimera-pipeline: Added Java 21 (Virtual Threads via `spring.threads.virtual.enabled=true`) + Spring Boot 3.x, Spring Data JPA, Spring Cache, Spring WebClient,
